// import { fromJS } from 'immutable';
// import { makeSelectMessidgeDomain } from '../selectors';

// const selector = makeSelectMessidgeDomain();

describe('makeSelectMessidgeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
